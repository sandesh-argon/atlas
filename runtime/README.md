# Runtime

Runtime contains production-serving logic for Atlas v31.

- `runtime/api/` — FastAPI server and endpoint contracts
- `runtime/simulation/` — graph loading, propagation, temporal simulation, and regional support

Start API locally:

```bash
cd runtime/api
python main.py
```
